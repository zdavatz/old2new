# Real-ESRGAN binary and models (downloaded at runtime)
realesrgan/

# Job working directories (frames, downloads)
jobs/

# vast.ai batch state
.vast_batch/
enhanced_videos/

# YouTube OAuth credentials (sensitive)
client_secret.json
youtube_token.json

# TikTok OAuth credentials (sensitive)
tiktok_credentials.json
tiktok_token.json

# LinkedIn OAuth credentials (sensitive)
linkedin_credentials.json
linkedin_token.json

# X / Twitter OAuth credentials (sensitive)
twitter_credentials.json
twitter_token.json

# Data Portability OAuth credentials (sensitive). yt_export_state.json holds the
# signed Cloud Storage URLs, which grant access to the archive on their own.
client_secret_dataportability.json
dataportability_token.json
yt_export_state.json

# Exported video masters (large)
exports/

# Temp/test files
*.zip
status_server_rs/target/
youtube_upload_rs/target/
tk_push_rs/target/
li_push_rs/target/
create_short_rs/target/
auth/

# Python bytecode
__pycache__/

# LinkedIn analytics exports and the joined output (personal performance data,
# regenerated on demand by li_stats.py — not source)
AggregateAnalytics_*.xlsx
linkedin_video_stats.csv

# Gmail API OAuth credentials (sensitive)
client_secret_gmail.json
gmail_token.json
