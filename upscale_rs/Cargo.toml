[package]
name = "upscale_rs"
version = "0.1.0"
edition = "2021"

[dependencies]
ort = { version = "2", features = ["cuda"] }
image = "0.25"
clap = { version = "4", features = ["derive"] }
ndarray = "0.16"

[profile.release]
strip = true
lto = true
